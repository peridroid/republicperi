package ru.devtron.republicperi.ui.views.adapter;

/**
 * interface to pass the event up the chain
 */
public interface OnRemoveItemListener {

    /**
     * On remove item.
     *
     * @param position the position
     */
    void onRemoveItem(int position);
}
